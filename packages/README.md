# packages

Recipes for everything GHL is made of. `ampkg` turns these into installable `.ampkg` archives.

Nothing here yet — the recipe format gets figured out in Phase 1 of the roadmap. Once it exists, the recipes will live here:

- `base` — everything a bootable GHL needs
- `init` — the init system
- `ampkg` — the package manager
- `clone-hero` — the whole reason this exists
